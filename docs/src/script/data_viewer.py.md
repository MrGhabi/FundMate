src/script/data_viewer.py
    功能描述: 命令行查看器，读取已落盘的 cash_summary/positions 结果并打印。

    display_cash_summary(data, date), display_positions(data, date)
        功能: 按日期打印现金/持仓的摘要。

    main()
        功能: CLI 入口，加载 result 目录数据并调用显示函数。
